/*const multiply = (number1, number2) => {
    return number1 * number2
}


const average = (number1, number2, number3) => {
    return (number1 + number2 + number3) /3
}

const converter = celsius =>{
    return (celsius*9/5)+32
}

beforeAll(()=>{
    console.log("Me ejecuté de primero")
})
beforeEach(()=>{
    console.log("Estoy antes de cada test")
})

afterAll(()=>{
    console.log("Estoy de último")
})

afterEach(()=>{
    console.log("Estoy debajo de cada test")
})

test("Multiply 2*2 should return 4", () => {
    expect(multiply(2, 2)).toBe(4)
})

test("Average 2+2+2 should return 2", () => {
    expect(average(2, 2, 2)).toBe(2)
})

test("Converter 20 celsuis should return 68 fhrenheit", () => {
    expect(converter(20)).toBe(68)
}) */